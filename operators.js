/*let person = (arg1) => {
  return arg1;
};

function example(age) {
  return age > 21 ? "allow" : "not allow";
}

let abc = example(person(25));

console.log(abc);*/

/*let array = [102, 104, "affruddin", "kavya", 107, 110, 104, 20.5, "Durga"];

console.log(array.length);
console.log(array.join("-"));
console.log(array.indexOf(20.5));
console.log(array.concat([20]));
console.log(array.includes(104));
console.log(array.slice(4));
console.log(array.slice(3, 5));
console.log(array.push("kavya"));
console.log(array.pop());
console.log(array);

let array = [
  "java",
  "phython",
  ".det",
  "java",
  "nodejs",
  "react",
  "angular",
  "angular",
];

let temp = [];
array.map((values) => {
  if (temp.includes(values) == false) {
    temp.push(values);
  }
});
console.log(temp);

var thought = "I am learning react";

if (thought.indexOf("react") !== -1) {
  console.log("react exists");
} else {
  console.log("not exists");
}

var url = "http://proclink.com";

if (url.indexOf("http://") == 0 || url.indexOf("https://") == 0) {
  console.log("particular url");
} else {
  console.log("not a particular url");
}

var url = "http://www.proclink.in";
var url1 = "http://microsoft.com";
var url2 = "http://google.org";


//finding the extension of url and domain of url.
var url = url.split(".");

console.log(url[url.length - 1]);
console.log(url[url.length - 2]);
console.log(url[url.length - 3]);*/

var date = "29-11-1994";

var data = date.split("-").reverse().join("-");

//console.log(data.reverse());

//console.log(data.join("-"));

console.log(data);

//var data = "1234raju7890";

//console.log(parseInt(data));

/*var data = data.split("");
num = "";
date.map((char) => {
  if (isNaN(parseInt(char)) == false) {
    num = num + char;
  }
});
console.log(num);

var string = "raju";

let newString = string.split("");
console.log(newString);

console.log(newString.reverse().join(""));

let details = {
  name1: "raju",
  age: 24,
  address: "jangaon",
  education: { degree: "bsc", inter: "mpc" },
};
//console.log(details.address1?.length);

//delete details.education.inter;

console.log(details);*/
